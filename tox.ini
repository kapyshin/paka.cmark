[tox]
envlist = py312
test_args = -m unittest discover --start-directory tests/
code_path = paka/*/

[testenv]
extras = testing
commands =
    python --version
    python {[tox]test_args}

[testenv:coverage]
usedevelop = true
basepython = python3.12
deps = coverage
commands =
    python3 --version
    coverage run --rcfile={toxinidir}/.coveragerc {[tox]test_args}
    coverage html -d {envtmpdir}/cov_html

[testenv:docs]
usedevelop = true
basepython = python3.12
deps = sphinx
commands =
    python3 --version
    sphinx-build -b html -W -n -d {envtmpdir}/doctrees docs/ {envtmpdir}/docs_html

[testenv:checks]
usedevelop = true
basepython = python3.12
allowlist_externals = sh
deps =
    flake8
    pydocstyle
    pylint
commands =
    python3 --version
    sh -c 'flake8 {[tox]code_path}'
    sh -c 'pydocstyle {[tox]code_path}'
    sh -c 'pylint {[tox]code_path}'
